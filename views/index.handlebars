<h1>Eat Da' Burger!!</h1>

<h2>These burgers have not been devoured!</h2>

<ul>
  {{#each burgers}}
    {{#unless devoured}}
    {{!-- This connects to the partials folder, specially burgers-block --}}
      {{> burgers/burger-block devour=true}}
    {{/unless}}
  {{/each}}
</ul>

<h2>These Burgers have been DEVOURED</h2>

<ul>
  {{#each burgers}}
    {{#if devoured}}
    {{!-- This connects to the partials folder, specially burgers-block --}}
      {{> burgers/burger-block devour=false}}
    {{/if}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-form">

  <div class="form-group">
    <label for="ca">Burger Name:</label>
    <input type="text" id="ca" name="burger_name">
  </div>

  <div class="form-group">
    <label for="slee">Have you ate it?</label><br>
    <input type="radio" name="devoured" value="1" checked> I already ate this burger, sorry!<br>
    <input type="radio" name="devoured" value="0"> Not yet!
  </div>

  <button type="submit">Add Burger</button>
</form>